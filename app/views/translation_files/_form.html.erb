<%= form_for(@translation_file) do |f| %>
  <% if @translation_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@translation_file.errors.count, "error") %> prohibited this translation_file from being saved:</h2>

      <ul>
      <% @translation_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :file %><br>
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
